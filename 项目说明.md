# PronFlow 项目全览说明

> 场景化口语训练 APP · 专治口语失语：心里有中文，开口就是英文
> 本文档帮你理清整个项目：两端 + 一云，技术栈、功能、部署、待办，一网打尽。

---

## 一、项目组成（一云两端）

```
                    ┌─────────────────────────┐
                    │   EdgeOne 云端（后端）    │
                    │  · 静态网页托管（网页版）  │
                    │  · 边缘函数 API（登录/    │
                    │    同步/词典/发音代理）    │
                    │  · KV 数据库（账号+数据）  │
                    └───────────┬─────────────┘
              HTTPS ↙                    ↘ HTTPS
    ┌──────────────────┐          ┌──────────────────┐
    │  网页版（PWA）     │          │  微信小程序版      │
    │  电脑/手机浏览器    │          │  微信内打开        │
    │  可添加到主屏幕    │          │                  │
    └──────────────────┘          └──────────────────┘
    数据存本机 localStorage        数据存本机 wx.Storage
    （登录后额外同步云端）           （登录后额外同步云端）
```

**核心理解：**
- 两个端是**独立应用**，各自存数据（互不相通，靠登录同一个账号 + 云同步合并）
- 云端只做两件事：**托管网页文件** + **提供账号/数据 API**
- 不登录也能用全部本地功能；登录 = 开启云备份和跨设备恢复

---

## 二、技术栈明细

### 网页版（PWA）
| 项 | 技术 |
|---|---|
| 框架 | Vite 5 + React 18 + TypeScript |
| 样式 | 手写 CSS（极简学习风，无 UI 库） |
| 状态 | React Context（UserDataProvider）+ localStorage 持久化 |
| 发音 | 双音源：有道云端真人声（默认）/ 系统 TTS，自动兜底 |
| 离线 | Service Worker（pronflow-v2 缓存应用壳+词库）+ manifest |
| PDF | 打印视图方案（矢量文字、可复制、文件小） |
| 部署 | EdgeOne Pages（上传 pronflow-deploy.zip 或连 Git） |

### 微信小程序版
| 项 | 技术 |
|---|---|
| 框架 | Taro 4.0.9 + React 18 + TypeScript（编译到微信小程序） |
| 发音 | 内置音频（innerAudioContext）：单词=有道真人录音；句子=3词一段分段连播；可选微信同声传译插件整句合成 |
| 录音 | RecorderManager（跟读录音回放） |
| 词典/联想 | 调云端 /api/dict 接口（词库太大不放包内） |
| 存储 | Taro.Storage（结构与网页版一致） |
| 构建 | `npm run build:weapp` → 产物 mp/dist → 微信开发者工具打开 |

### 云后端（EdgeOne 边缘函数，全免费额度）
| 端点 | 功能 |
|---|---|
| POST /api/auth/register | 注册（浏览器端已加密的密码哈希） |
| POST /api/auth/login | 登录 → 返回 30 天 token |
| GET /api/data | 拉取云端数据（Bearer token） |
| PUT /api/data | 推送本地数据（整体覆盖，改动后自动推） |
| GET /api/dict | 词典查询（12000 词内嵌在函数里，无出站依赖） |
| GET /api/tts | 发音代理（单词→有道，句子→百度，服务器侧取音频绕防盗链） |
| GET /api/ping | 音源连通性诊断 |

**架构原则（低耦合）**：前端只认 REST 契约，EdgeOne 只是第一版实现；换平台只需照契约重写 `edge-functions/`（约 200 行），前端零改动。

---

## 三、功能清单

### 两端共有
- ✅ 20 个生活场景 · 600+ 条口语素材（全部人工编写）
- ✅ 每条素材：美/英双音标 · 口语化翻译 · 1-3 条对话例句 · 简单/日常/地道多版本 · 口语度标注
- ✅ 句子：长句拆分（逐段开口）+ 连读/弱读/吞音标注
- ✅ 训练三模式：中文遮屏自测（卡住自动进次日复习）/ 慢速跟读 / 句式仿写（槽位造句）
- ✅ 中文搜英文（模糊/精准、历史、分组结果）
- ✅ 收藏 + 每日复习（间隔重复：说出间隔翻倍、卡住明天再来，上限 20 条/日）
- ✅ 自定义场景 CRUD + 置顶 + 图标；自定义素材全字段增删改（内置场景也能补充）
- ✅ 跟读录音回放
- ✅ 登录 + 云同步（自动推送 + 合并拉取）
- ✅ 无广告无弹窗 · 数据自主

### 网页版独有
- ✅ 独立大词库页（18746 词，四六级/考研/雅思/托福分级 + 词频）
- ✅ PDF 卡片导出（矢量、可打印）
- ✅ PWA 离线（添加到主屏幕，断网可用）
- ✅ 音源切换设置（云端人声/系统人声）

### 小程序版独有
- ✅ 卡片长图保存到相册
- ✅ 分段连播句子音源（有道短语级真人录音）

---

## 四、目录结构

```
PronFlow/
├── src/                    # 网页版源码
│   ├── data/scenes/        # ★ 20 场景数据（核心内容，每场景一个文件）
│   ├── services/           # tts 发音 / search 搜索 / sync 云同步 / dict 词库
│   ├── store/              # 用户数据层（localStorage）
│   ├── components/         # 素材卡片/播放按钮/表单/打印视图等
│   └── pages/              # 8 个页面
├── public/                 # 网页静态资源（dict.json 词库 / SW / 图标）
├── edge-functions/         # ★ 云后端（随部署包一起上传）
│   └── api/
│       ├── [[route]].js    # 认证/数据同步/tts 代理/ping
│       └── dict.js         # 词典查询（自动生成的 12000 词内嵌数据）
├── mp/                     # ★ 微信小程序版（独立 Taro 工程）
│   ├── src/pages/          # 9 个页面
│   ├── src/shared/web/     # ★ 与网页版共享的逻辑（sync-shared 同步）
│   ├── src/store/          # MP 存储层
│   ├── src/services/       # MP 发音/同步
│   ├── config/             # Taro 构建配置
│   └── dist/               # 编译产物（开发者工具读取，已 gitignore）
├── scripts/                # 工具脚本（见下方命令速查）
├── pronflow-deploy.zip     # 网页部署包（package 生成）
└── README.md               # 快速上手
```

---

## 五、常用命令速查

### 网页版
```bash
npm install              # 首次安装依赖
npm run dev              # 本地开发 http://localhost:5180
npm run build            # 构建生产版本
npm run package          # 打包部署 zip（pronflow-deploy.zip）
npm run dict             # 重新生成大词库（需联网）
```

### 共享代码（网页 → 小程序 单向同步）
```bash
npm run sync-shared      # 网页版数据/逻辑变更后执行
```

### 小程序
```bash
cd mp
npm install              # 首次安装
npm run build:weapp      # 构建（产物 mp/dist）
npm run dev:weapp        # 监听模式（改代码自动重建）
npm run build:weapp:si   # 插件版构建（启用微信同声传译，需正式 AppID）
```

### 以后每次更新版本的完整流程
```
1. 改代码（网页改完记得 npm run sync-shared）
2. npm run build && npm run package   → 上传 pronflow-deploy.zip 到 EdgeOne
3. cd mp && npm run build:weapp       → 开发者工具重新编译 / 上传
```

---

## 六、部署指南

### 网页版 + 云后端（EdgeOne Pages，一体部署）
1. 登录 EdgeOne Pages 控制台 → 进入 pronflow 项目
2. 「直接上传」→ 选择 `pronflow-deploy.zip`（每次 `npm run package` 重新生成）
3. 部署完成得到访问网址（控制台复制最新链接；预览链接带 token 会过期）

**一次性设置（已做/待办见第八节）：**
- KV 存储 → 绑定命名空间 → **变量名必须填 `pf_kv`**（登录/同步依赖）

### 小程序（微信开发者工具）
1. 安装微信开发者工具 → 导入项目 → 目录选 `PronFlow/mp`
2. AppID：开发阶段用「测试号」；正式发布需注册小程序账号（免费）替换
3. 「详情 → 本地设置」→ 勾选「**不校验合法域名**」（开发期必需）
4. 点「编译」预览；「预览」扫码真机测试

### 小程序正式发布（面向公众）
1. 注册小程序账号 → AppID 替换 `mp/project.config.json` 里的 `touristappid`
2. mp 后台配置服务器域名（**必须 ICP 备案域名**）：
   - request：EdgeOne 网址（需确认备案状态）+ `https://fanyi.baidu.com`
   - downloadFile：`https://dict.youdao.com` + `https://fanyi.baidu.com`
3. 开发者工具「上传」→ mp 后台「版本管理」→ 提交审核（1~2 天）→ 发布
4. 想要整句发音：mp 后台添加「同声传译」插件（⚠️ 个人主体搜不到，需企业主体）→ `npm run build:weapp:si` 构建

---

## 七、数据与同步规则

- **本地优先**：所有数据先存设备本地，离线可用
- **登录 = 开启云备份**：改动后 2.5 秒自动推送云端；登录/手动同步时拉取云端并**合并**（并集 + 新者胜，不丢任何一侧数据）
- **已知限制**：删除操作暂不跨设备同步（建议在常用设备上删除）
- **Token 有效期 30 天**，过期重新登录即可
- 隐私：密码 PBKDF2/SHA256 加密后存储，明文永不离开设备；不登录则零数据上云

---

## 八、当前待办与已知限制（截至本次交付）

### 待办（按优先级）
| # | 事项 | 谁 |
|---|---|---|
| 1 | EdgeOne **重新上传最新 pronflow-deploy.zip**（含词典接口/音源修复/tts 代理——线上还是旧包） | 你 |
| 2 | EdgeOne 控制台 **绑定 KV，变量名 `pf_kv`**（登录功能依赖） | 你 |
| 3 | 绑定后告知 → 远程验收登录/同步/词典全链路 | 我 |
| 4 | 小程序真机预览测试（扫码） | 你 |

### 已知限制
| 项 | 说明 |
|---|---|
| 有道接口不支持长句 | 已用「百度合成（网页）/ 分段连播（小程序）」绕开 |
| 小程序百度源被 Referer 拦 | 已用 /api/tts 代理绕开（需部署最新包） |
| 删除不跨设备同步 | 合并策略保数据不丢，代价是删除仅本机生效 |
| PDF 导出 | 网页版支持；小程序用「长图存相册」替代 |
| 整句自然发音（小程序） | 需企业主体才能加同声传译插件，个人用分段连播 |

### GitHub 仓库
- 已推送：https://github.com/cy547/PronFlow（main 分支）
- 推送走本地 7897 代理（git config http.proxy 已设置，代理需保持开启）
