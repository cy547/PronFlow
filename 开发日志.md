# PronFlow 开发日志与交接文档

> 最后更新：2026-08-30 · 由 ZCode（AI）从零开发 · 本文目标：**任何开发者/AI 读完本文即可接手继续开发**
> 配套阅读：`项目说明.md`（面向使用者的项目全览）、`README.md`（快速上手）

---

## 0. 快速接手清单（5 分钟版）

1. 项目 = **网页版 PWA**（Vite+React，部署在 EdgeOne Pages）+ **微信小程序版**（Taro 4+React）+ **云后端**（EdgeOne 边缘函数+KV，随网页部署自带）
2. 产品定位：**不做背单词**，专治「心里有中文说不出英文」——一切功能围绕场景开口
3. 数据单一来源：`src/data/`（20 场景素材）→ `npm run sync-shared` 镜像到小程序
4. 当前待办见 [第 10 节](#十待办事项)，改动后自测见 [第 11 节](#十一改动后自测清单)
5. **用户环境限制**：中国大陆网络（GitHub/外网需走本地代理，端口 7897）；用户偏好中文交流、简单解释

---

## 1. 项目定位与当前状态

| 维度 | 内容 |
|---|---|
| 产品 | 场景化口语训练：20 个生活场景 × 600+ 条口语素材（词/短语/句子），训练三模式（遮屏自测/慢速跟读/句式仿写），搜索，收藏复习，登录云同步 |
| 网页版状态 | ✅ 已部署 EdgeOne（项目名 pronflow，最新部署 URL 前缀 `pronflow-dpu548yj649n`；**每次直接上传 zip 会生成新项目 URL**） |
| 小程序状态 | ✅ 代码完成，DevTools 编译可用；**未注册正式 AppID**（用测试号 touristappid），未真机验证 |
| 云后端状态 | ⚠️ 边缘函数已部署生效，但 **KV 未绑定（变量名 `pf_kv`）** → 登录/同步接口报 500 `pf_kv is not defined`，等用户在控制台绑定 |

---

## 2. 环境信息（开发者本机，Windows）

- Windows 10/11 + Git Bash；Node v22.14.0；npm registry = npmmirror
- **本地代理端口 7897**（访问 GitHub 必须走它，已写入 `git config http.proxy`；代理关了 push 会 Connection reset）
- EdgeOne 部署方式：控制台「直接上传」zip（用户手动操作）；CLI `edgeone` 已全局安装（登录/部署也可用）
- 微信开发者工具 Stable 1.06.24，基础库 3.4.10
- ⚠️ Windows Git Bash 下 `taskkill //F //PID <pid>` 才能杀干净后台 node 进程（kill 管道任务杀不干净）

---

## 3. 命令速查

```bash
# ── 网页版（根目录）──
npm run dev            # 开发 http://localhost:5180
npm run build          # 构建 dist/
npm run package        # 生成 pronflow-deploy.zip（上传 EdgeOne 用）
npm run dict           # 重新生成 public/dict/dict.json（ECDICT+CMU，需联网装好的包）
npm run sync-shared    # ★ 把 src/{types,data,services/search,services/suggest} 镜像到 mp/src/shared/web/

# ── 小程序（cd mp）──
npm run build:weapp     # 构建产物 mp/dist（微信开发者工具读取）
npm run dev:weapp       # 监听构建
npm run build:weapp:si  # 插件版构建（启用微信同声传译，需正式 AppID+已添加插件）

# ── 词典再生成 ──
npm run gen-dict-mp     # 小程序内置词典（前 7000 词，紧凑字符串 → mp/src/data/dictData.ts）
npm run gen-dict-edge   # 边缘函数词典（前 12000 词 → edge-functions/api/dict.js）※当前未使用
```

---

## 4. 架构与数据流

### 网页版
```
src/pages/*（页面） → src/components/*（UI） → src/services/*（tts/search/sync/dict）
                                                ↓ 数据
                                      src/store/UserDataProvider.tsx
                                        （Context + localStorage 持久化）
                                                ↓ 变更事件 'pf-data-changed'
                                      src/services/sync.ts（防抖推送云端）
```
- **数据单一来源**：`src/data/index.ts` 聚合 20 个场景 bundle；`searchAll` 服务扫它 + 用户自定义素材
- **登录同步契约**（平台无关，换后端只重写 server）：
  - `POST /api/auth/register|login {username, pwdHash}` → `{token, username}`
  - `GET /api/data`（Bearer）→ `{data: UserData|null, updatedAt}`
  - `PUT /api/data {data, updatedAt}` → 整体覆盖
  - 密码在浏览器 PBKDF2(SHA256, 120k, salt=sha256("pronflow:"+username)) 后传输，服务端只存哈希
- **合并策略**：并集 + 新者胜（favorites 比 addedAt、review 比 updatedAt、stats 取 max）；**删除不跨设备同步**（无墓碑，v1 决策）

### 小程序版
```
mp/src/pages/* → mp/src/store/store.ts（模块级单例 + 订阅） → Taro.Storage
mp/src/shared/web/* ← scripts/sync-shared.cjs 镜像 ← 根/src/{types,data,services/search,services/suggest}
mp/src/services/audio.ts：发音（有道单词录音 / 分段连播 / 插件）
mp/src/services/sync.ts：API_BASE 写死当前 EdgeOne 项目 URL（★ 换部署项目必改）
```
- **跨工程复用方案**：不跨工程 import（webpack 编译外部目录踩坑），用 `sync-shared.cjs` **镜像拷贝**，保持相对结构使内部 import 天然兼容
- **MP 无 React Context 跨页状态**：store.ts 模块单例 + `useUserData()`（useState+subscribe）

### 发音决策链（★ 最复杂、踩坑最多的模块）
```
播放(text)：
网页版：
  voiceSource 设置（默认 cloud）：
    cloud：单词(≤3词)→有道真人录音；句子→百度神经语音（网页 no-referrer 可直连百度）
           任一失败 → 系统 TTS（1.2s 快速失败判定）→ 都失败弹提示
    system：系统 TTS → 静默失败(1.2s 无 onstart) → 云端兜底
小程序版：
  1) 微信同声传译插件（若构建时声明且运行时可用）→ 整句合成
  2) 兜底：单词(≤3词)→有道；句子→按 3 词切段的有道短语录音顺序连播
     （小程序 InnerAudio 强制带 servicewechat Referer → 百度必被拒，故不用于 MP）
  每段失败自动跳下一段；全部失败 toast
```

---

## 5. 关键文件地图（改什么动哪里）

| 要改的东西 | 文件 |
|---|---|
| 场景素材内容（词/短语/句） | `src/data/scenes/<scene>.ts`（一场景一文件，改完 `npm run sync-shared`） |
| 新增场景 | 加 `src/data/scenes/xxx.ts` + 在 `src/data/index.ts` BUNDLES 注册 + sync-shared |
| 口语增强词（词典页的例句/替换） | `src/data/spokenEnrichment.ts`（ROWS 元组表，322 条） |
| 发音逻辑/音源 | `src/services/tts.ts`（网页）、`mp/src/services/audio.ts`（小程序，逻辑不同步） |
| 同步契约/合并策略 | `src/services/sync.ts`（前端）、`edge-functions/api/[[route]].js`（后端）、MP 版 `mp/src/services/sync.ts`（镜像手写） |
| 搜索算法 | `src/services/search.ts` |
| 复习调度 | `src/store/UserDataProvider.tsx`（upsertReview）/ `mp/src/store/store.ts` |
| 内置离线词典（MP） | `npm run gen-dict-mp` 生成 `mp/src/data/dictData.ts`；词数在脚本里 slice(0, 7000) |
| 大词库再生成 | `npm run dict`（ECDICT+CMU）→ `npm run gen-dict-edge`（如需边缘词典） |
| 部署包 | `npm run package`（jszip 生成，正斜杠路径——**不能用 PowerShell Compress-Archive**，见踩坑 #9） |
| 小程序后端地址 | `mp/src/services/sync.ts` 的 `API_BASE`（★ 换 EdgeOne 项目/域名必改） |
| PWA 缓存版本 | `public/sw.js` 的 VERSION 常量（改静态资源后 bump） |

---

## 6. 数据契约速查

### UserData（localStorage key `pronflow-userdata-v1` / MP 同名）
```ts
{ settings: {accent:'US'|'UK', rate:'normal'|'slow', voiceSource:'cloud'|'system'},
  pinned: string[], customScenes: Scene[], customMaterials: Material[],
  favorites: Record<'kind:id', {key,kind,addedAt}>,
  review: Record<'kind:id', ReviewItem/*含 updatedAt*/>,
  history: string[], stats: {tests,reviews,streak,lastActive} }
```
- 素材 id 规则：内置 `<scene>-w1/p1/s1`；自定义 `cw-/cp-/cs-<timestamp>`
- 收藏 key：`word:xxx` / `phrase:xxx` / `sentence:xxx` / `dict:单词`

### Material 统一 schema（网页/小程序一致）
见 `src/types.ts`。所有素材必含：en, zh, spokenLevel(high/ok/formal), examples[1-3], variants[简单/日常/地道]；句子加 breakdown + linking（`‿`连读 `*`弱读 `(x)`吞音）；部分句子有 template（仿写）。

### 发音音源链（网页）
`cloud` 源：单词≤3词→youdao；句子→baidu；失败→系统 TTS（1.2s 快速失败）→提示。
`system` 源：系统 TTS → 静默失败 → 云端。详见 `src/services/tts.ts` play()。

---

## 7. 设计决策记录（为什么这么做）

| 决策 | 原因 |
|---|---|
| 发音用系统 TTS + 云端音频双源 | 无法内置真人音频文件；系统 TTS 离线可用但安卓引擎质量参差；云端音质稳但需联网 |
| 词典 7000 词内置小程序 + 12000 词边缘函数 | 小程序主包 2MB 限制（内置版占 432KB 源码）；边缘版服务网页（网页本地已有全量，边缘版当前未使用，留作 API） |
| 边缘函数 /api/tts 代理 | 为绕百度 Referer 防盗链设计；**实测 EdgeOne 边缘函数出站 fetch 崩溃（545）已废弃**，但保留代码备查 |
| sync-shared 镜像而非跨工程 import | webpack 编译工程外目录配置复杂且不稳，镜像+脚本确定性最高 |
| 登录用用户名密码而非微信登录 | 微信 code2Session 需要 AppSecret 放服务端+正式 AppID；用户名密码在网页/小程序通用（同一套 REST 契约） |
| 删除不跨设备同步 | 无墓碑机制的 union 合并下，跨设备删除会复活；v1 保数据不丢优先 |

---

## 8. 踩坑记录（★ 接手必读，全是实测教训）

### 网络与部署
1. **有道 dictvoice 单词/短语 OK，句子 500**（`{"msg":"returned null audio"}`）。阈值与**内容相关**：`hello world`✅ `good morning teacher`✅（3词）`could you help me find it`❌（6词）`a cheeseburger and`❌（3词但语法碎片）。→ 网页端句子用百度；MP 端分段连播 + **每段去掉尾部标点**（`go, please.` 带 `,.`, 也 500）
2. **百度 gettts 有 Referer 防盗链**：无 Referer→200 audio/mpeg；带**任何** Referer（localhost/edgeone）→200 但空 text/html。网页端靠 `<meta name="referrer" content="no-referrer">` 解决；**小程序强制带 servicewechat referer，无解 → 百度在 MP 不可用**
3. **EdgeOne 边缘函数出站 fetch 会崩**：`/api/ping` 只做两个外部 fetch 就 545 `Error return from script`（免费环境限制）。→ `/api/tts` 代理方案废弃（代码保留在 `[[route]].js`）
4. **EdgeOne 预览链接带 eo_token/eo_time 有时效**（数小时）；正式域名要看控制台。curl 访问 EdgeOne 域名会被防护拦（401/302 challenge），**测试必须用真实浏览器会话**（IAB/DevTools）
5. **PowerShell Compress-Archive 打的 zip 是反斜杠路径** → EdgeOne 上传报「文件名称包含非法字符」。**必须用 jszip**（`npm run package`）
6. **GitHub 直连 Connection reset** → 走本地代理 7897（`git config http.proxy http://127.0.0.1:7897`）。ECDICT 原始数据 GitHub 也连不上，但 npm 镜像有 `ecdict` 包（69MB 含全量 csv）✓
7. **EdgeOne /api/* 测试方法**：curl 会 401，必须在真实浏览器页里 `fetch()`（借已过防护的会话）

### 网页版
8. 搜索「AA制」失败 → 分词必须**中英文混合拆段**（regex `[a-z0-9']+|[\u4e00-\u9fff]+`），且 template 文本要进搜索索引
9. 复习流 bug：评分后条目移出 due 数组导致提前结束 → **开始复习时快照固定队列**
10. React hook 违规：条件渲染分支里调 hook（useStreak）会崩 → hook 全部提前到组件顶部
11. 样式类名与 CSS 对不上（TSX 升级了 CSS 没跟上）→ 首页卡片裸奔。改 TSX 时同步检查 CSS

### 小程序（Taro/微信）
12. **Taro 手动脚手架缺 babel 依赖**，需补：`@babel/preset-react` `@babel/preset-typescript` `@babel/plugin-proposal-class-properties` `@babel/plugin-proposal-decorators` `@babel/plugin-transform-runtime` `@babel/plugin-transform-typescript` `@babel/plugin-proposal-optional-chaining` `@babel/plugin-proposal-nullish-coalescing-operator` `@babel/plugin-proposal-object-rest-spread` `@types/node`
13. **微信主包限 2MB**：词典 JSON 数组格式编译后 2.05MB 超限 → **紧凑单字符串格式**（`a|b|c|d|e|1\n...` 运行时 split）降到 1.82MB/7000 词
14. **微信 tabBar 页面必须在主包** → 词库页不能放分包还挂 tab；离线方案改为词典内置主包
15. **微信只认 .wxss**：Taro prod 配置里手写 `miniCssExtractPluginOption.filename: '[name].css'` 会导致样式全丢 → **不要覆盖**，默认输出 .wxss
16. **MP store 导出缺失白屏**：`getUserData is not a function` → `useUserData.ts` 必须 `export { getUserData, actions, ... } from './store'`（页面都从它导入）
17. **小程序发音请求强制带 servicewechat referer** → 百度防盗链必拒（见踩坑 2），MP 只能有道直连或分段
18. **DevTools 热重载不刷新 wxss** → 改样式后手动点「编译」
19. `Taro.showActionSheet` 用户取消会走 fail → 加 `fail: () => {}` 否则报错刷屏
20. **EdgeOne 边缘函数 KV 的 key 只允许字母/数字/下划线**（不能用冒号连字符）；KV 绑定变量名（pf_kv）必须与代码一致；**每个新部署项目都要重新绑定 KV**

### 小程序功能限制（现状即限制）
21. 有道短语合成内容相关失败率高 → MP 句子最终方案 = 同声传译插件（需正式 AppID+企业主体？个人搜索不到，见下）或分段连播
22. **「同声传译」插件个人主体搜索不到**（插件市场搜「同声传译」无结果；试过 AppID 精确搜未验证）→ 插件版构建 `build:weapp:si` 已就绪但用户暂不可用
23. 用户腾讯云密钥曾在聊天中泄露过（SecretId/SecretKey），**未写入任何项目文件**；已提醒用户去 CAM 禁用

---

## 9. 已知限制（当前版本）

- 删除不跨设备同步（见决策表）
- MP 句子发音：默认分段连播（有节奏感）；整句自然发音需插件+企业主体
- MP 词库 7000 词（离线）；网页 18746 词（本地全量）
- MP 无：PDF 导出（可用长图）、图标上传（v1 用 emoji）——等等，图标上传已做 ✓；PDF 未做
- 网页 localhost 开发时云端接口不可用（/api 只有部署后存在），登录/词典/云端发音需部署后测试

---

## 10. 待办事项

| # | 事项 | 谁 | 备注 |
|---|---|---|---|
| 1 | EdgeOne **重新上传最新 pronflow-deploy.zip**（含 /api/dict、音源设置、句子百度直连、移除失效代理） | 用户 | 上传后强刷页面 |
| 2 | EdgeOne 控制台 **绑定 KV（变量名 `pf_kv`）** | 用户 | 绑定后登录立即可用；若即时未生效再触发一次部署 |
| 3 | 远程验收：注册/登录/数据同步/词典/发音全链路 | AI | 用户完成 1、2 后 |
| 4 | MP 真机预览（录音/发音真机表现） | 用户 | 扫「预览」二维码 |
| 5 | （可选）正式 AppID 注册 + 域名配置 + 小程序审核发布 | 用户 | 个人主体：同声传译插件不可用 |
| 6 | （远期）AI 对话陪练 / 学习统计 / 场景包分享 / FSRS | 双方 | 见 项目说明.md 第三梯队 |

---

## 11. 改动后自测清单

**网页版（npm run build + preview:4173 或 DevTools 打开 localhost:5180）**
- [ ] 首页 20 场景网格渲染、计数正确
- [ ] 场景详情三 Tab、卡片展开（例句/变体/拆分/连读）
- [ ] ▶ 播放（DevTools 无声但无报错弹窗 = 云源链路通）
- [ ] 训练三模式全走一遍（自测评分→复习清单+1）
- [ ] 搜索「AA制」「我不吃辣」「order」
- [ ] 词库页查「推迟」「grab」（需最新包部署后）
- [ ] 收藏 ☆ → 复习页出现 → 走完复习流
- [ ] 我的：设置切换、导出备份（剪贴板有 JSON）
- [ ] 登录注册（需 KV 绑定后）

**小程序（DevTools 编译）**
- [ ] 首页网格有样式（白卡片双列）
- [ ] 词库 Tab 离线查「order」「推迟」
- [ ] 场景快捷添加 → 联想弹窗 → 保存
- [ ] 训练三模式 + 录音回放
- [ ] 复习流 + 收藏本
- [ ] 长图导出（相册权限）
- [ ] Console 无红色报错

---

## 12. 历史版本节点

- v1.0 初版：网页全功能 + 小程序全功能 + EdgeOne 后端
- 迭代：搜索中英混合分词修复、复习队列快照修复、发音三重音源链、PWA 离线、录音回放、PDF 卡片导出、图标上传、词库联想（云+离线双版）、长图导出、登录云同步（EdgeOne KV）
- 外部依赖版本：Taro 4.0.9（全 @tarojs/* 同版）、Vite 5.4、React 18.3、TS 5.6、jszip 3.10（部署打包）
